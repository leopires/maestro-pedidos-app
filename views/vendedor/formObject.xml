<?xml version="1.0"?>
<!DOCTYPE view SYSTEM "../../../../public/layouts/view.dtd">
<view>
    <mform id="formObject" close=">pedidos/vendedor/formFind" base="formBase">
        <property title="$this->data->vendedor->description" />
        <action action="{{ 'formUpdate/' . $this->data->id }}" label="Editar" />
        <action action="{{ 'formDelete/' . $this->data->id }}" label="Excluir" />
        <action action="{{ 'formCarteiraClientes/' . $this->data->id }}" label="Carteira de clientes" />
        <action action="{{ 'formHistoricoPedidos/' . $this->data->id }}" label="Histórico de pedidos" />
    </mform>
</view>